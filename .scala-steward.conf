updates.pin = [
  { groupId = "org.slf4j", artifactId = "slf4j-api", version = "1." },
  { groupId = "ch.qos.logback", artifactId = "logback-classic", version = "1.2." },

  # 0.13 requires Java 11, and 0.12 still gets the job done fine.
  { groupId = "io.github.sbt-doctest", artifactId = "doctest-runtime", version = "0.12." },
  { groupId = "io.github.sbt-doctest", artifactId = "sbt-doctest", version = "0.12." },
]

updates.ignore = [ 
  # Causes spurious updates to deprecation versions
  { groupId = "org.http4s", artifactId = "http4s-scalafix-internal" }
]
